package com.project1.model;

public enum DataType {

    INTEGER,
    FLOAT,
    CHAR,  // might have to take this and
    VARCHAR,// varchar out depending
    TEXT,// on if I can easily limit
    BOOLEAN;// varchar(n) and char(n).

}
